// Small alias so dealer-facing components have a 1-line import without
// reaching into components/admin. Delegates to the admin StatusBadge.

export { StatusBadge as SubmissionStatusPill } from "@/components/admin/StatusBadge";
